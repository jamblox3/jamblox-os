the name is a work in progress

main.asm is the bootloader it will load the kernel into ram, switch to 32bit mode and jump to the kernel

kernel.c is not finished it is supposed to read from disk using i/o ports but i cant get it to work yet

compiler.bat is my c compiler script
